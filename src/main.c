#include <stdio.h>
#include <sys/types.h>

#include "utils.h"
#include "crypt4gh.h"
#include "cli.h"

void
cleanup_exit(int i)
{
  //sftp_server_cleanup_exit(i);
}

int
main(int argc, const char **argv)
{

  return 0;
  //return (sftp_server_main(argc, argv));
}
